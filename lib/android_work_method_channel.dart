/// @deprecated 旧入口，保留 import 路径兼容。
/// 实际实现见 `package:device_doctor/src/device_doctor_method_channel.dart`，
/// `MethodChannelAndroidWork` 为 `MethodChannelDeviceDoctor` 的 typedef 别名。
library android_work_method_channel_compat;

export 'src/device_doctor_method_channel.dart';
