/// @deprecated 旧入口，保留 import 路径兼容。
/// 类型现由 `package:device_doctor` 内部实现提供：
/// - `AndroidWorkPlatform` = `DeviceDoctorPlatform`（typedef）
/// - `MethodChannelAndroidWork` = `MethodChannelDeviceDoctor`（typedef）
library android_work_platform_interface_compat;

export 'src/device_doctor_platform_interface.dart';
